name: BHVA Contacts Sync

on:
  workflow_dispatch:

jobs:
  sync-contacts:
    runs-on: windows-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Install Microsoft Graph (NuGet fallback)
        run: |
          Install-Package -Name Microsoft.Graph -ProviderName NuGet -Scope CurrentUser -Force
          Import-Package -Name Microsoft.Graph
        shell: pwsh

      - name: Run sync script (DryRun)
        run: pwsh "./BHVA Contacts Sync.ps1" -DryRun:$true
        env:
          GRAPH_TENANT_ID: ${{ secrets.GRAPH_TENANT_ID }}
          GRAPH_CLIENT_ID: ${{ secrets.GRAPH_CLIENT_ID }}
          GRAPH_CLIENT_SECRET: ${{ secrets.GRAPH_CLIENT_SECRET }}
